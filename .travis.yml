language: cpp

os:
  - linux

compiler:
  - gcc
  - g++
  - clang

addons:
  apt:
    packages:
      - graphviz

before_install:
  - if [ $TRAVIS_OS_NAME = linux ]; then sudo add-apt-repository --yes ppa:beineri/opt-qt593-trusty; fi;
  - if [ $TRAVIS_OS_NAME = linux ]; sudo apt-get -qq update; fi;
#  - if [ $TRAVIS_OS_NAME = linux ]; sudo apt-get -qq install gcc; fi;
#  - if [ $TRAVIS_OS_NAME = linux ]; sudo apt-get -qq install g++; fi;
  - if [ $TRAVIS_OS_NAME = linux ]; sudo apt-get -qq install qt59quickcontrols2; fi;
  - if [ $TRAVIS_OS_NAME = linux ]; sudo apt-get -qq install ninja-build; fi;

install:

before_script:
  - source /opt/qt59/bin/qt59-env.sh
  - mkdir build
  - cd build

script:
  - cmake .. -DCMAKE_BUILD_TYPE=Release -G "Ninja"
  - ninja
#  - ninja install
  - ctest --output-on-failure
  - cpack

notifications:
  email:
    - asiniscalchi@gmail.com

